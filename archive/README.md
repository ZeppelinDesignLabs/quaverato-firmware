# Archived field builds

Original `.ino` + `.hex` pairs preserved for GitHub Releases and historical reference.
Do not edit. Checksums: [`docs/releases.md`](../docs/releases.md).

`master` on the remote also retains these files at the repository root as of the pre-cleanup history.
